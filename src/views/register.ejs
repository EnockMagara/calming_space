<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Register</title>
  <link rel="stylesheet" href="/styles.css">
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css">
  <% if (typeof baseUrl !== 'undefined') { %>
    <base href="<%= baseUrl %>/">
  <% } %>
</head>
<body>
  <div class="register-container">
    <h1 class="animate__animated animate__bounce">Register</h1>
    <% if (typeof error !== 'undefined' && error) { %>
      <div class="error-message"><%= error %></div>
    <% } %>
    <form action="<%= typeof baseUrl !== 'undefined' ? baseUrl : '' %>/register" method="post" class="animate__animated animate__bounce">
      <input type="text" name="username" placeholder="Username" required>
      <input type="password" name="password" placeholder="Password" required>
      <button type="submit">Register</button>
    </form>
    <p class="animate__animated animate__bounce">Already have an account? <a href="<%= typeof baseUrl !== 'undefined' ? baseUrl : '' %>/login">Login</a></p>
  </div>
</body>
</html>
